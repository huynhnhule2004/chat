# 🏗️ GROUP CHAT ARCHITECTURE VISUALIZATION

## 🎨 System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         CLIENT SIDE (Flutter)                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  ┌──────────────────┐    ┌──────────────────┐    ┌──────────────────┐  │
│  │ CreateGroupScreen│    │  GroupChatScreen │    │ JoinGroupDialog  │  │
│  │                  │    │                  │    │                  │  │
│  │ [Name Input]     │    │ [Message List]   │    │ [Password Input] │  │
│  │ [Password Toggle]│    │ [Send Input]     │    │ [Join Button]    │  │
│  │ [Member Select]  │    │ [Socket Listener]│    └──────────────────┘  │
│  └────────┬─────────┘    └────────┬─────────┘             │             │
│           │                       │                        │             │
│           └───────────────┬───────┴────────────────────────┘             │
│                           │                                              │
│                  ┌────────▼────────┐                                     │
│                  │ GroupKeyService │                                     │
│                  ├─────────────────┤                                     │
│                  │ • generateSessionKey()  (32 bytes random)            │
│                  │ • encryptSessionKey()   (RSA-OAEP)                   │
│                  │ • decryptSessionKey()   (RSA-OAEP)                   │
│                  │ • encryptMessage()      (AES-256-GCM)                │
│                  │ • decryptMessage()      (AES-256-GCM)                │
│                  └────────┬────────┘                                     │
│                           │                                              │
│           ┌───────────────┼───────────────┐                             │
│           │               │               │                             │
│  ┌────────▼────────┐  ┌──▼──────┐  ┌────▼──────────┐                  │
│  │ SecureStorage   │  │CryptoSvc│  │ DatabaseHelper │                  │
│  │ (Session Keys)  │  │(AES/RSA)│  │ (SQLite v3)    │                  │
│  └─────────────────┘  └─────────┘  └────────────────┘                  │
│                                                                          │
└──────────────────────────────┬───────────────────────────────────────────┘
                               │
                               │ HTTPS API + WebSocket
                               │
┌──────────────────────────────▼───────────────────────────────────────────┐
│                         SERVER SIDE (Node.js)                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│                    ┌───────────────────────┐                             │
│                    │   Express.js Server   │                             │
│                    │   + Socket.IO         │                             │
│                    └───────────┬───────────┘                             │
│                                │                                          │
│               ┌────────────────┼────────────────┐                        │
│               │                │                │                        │
│      ┌────────▼─────────┐  ┌──▼────────┐  ┌───▼──────────────┐         │
│      │  /api/groups/*   │  │ auth.js   │  │ SocketService    │         │
│      │  (routes)        │  │(JWT check)│  │ (Real-time)      │         │
│      │                  │  └───────────┘  │                  │         │
│      │ • POST /create   │                 │ • join_group     │         │
│      │ • POST /join     │                 │ • send_group_msg │         │
│      │ • POST /kick     │                 │ • receive_group  │         │
│      │ • GET  /:id      │                 │ • key_rotated    │         │
│      └────────┬─────────┘                 └───────┬──────────┘         │
│               │                                   │                     │
│               └───────────────┬───────────────────┘                     │
│                               │                                         │
│                  ┌────────────▼──────────────┐                          │
│                  │  GroupKeyService.js       │                          │
│                  ├───────────────────────────┤                          │
│                  │ • generateSessionKey()    │                          │
│                  │ • encryptSessionKey()     │                          │
│                  │ • encryptForMembers()     │                          │
│                  │ • rotateSessionKey()      │                          │
│                  └────────────┬──────────────┘                          │
│                               │                                         │
│               ┌───────────────┼───────────────┐                         │
│               │               │               │                         │
│      ┌────────▼────────┐  ┌──▼────────┐  ┌──▼──────────┐              │
│      │  Room.js        │  │RoomMember │  │ Message.js  │              │
│      │  (Model)        │  │  (Model)  │  │  (Model)    │              │
│      └────────┬────────┘  └───┬───────┘  └──┬──────────┘              │
│               │               │              │                         │
└───────────────┼───────────────┼──────────────┼─────────────────────────┘
                │               │              │
                └───────────────┼──────────────┘
                                │
                    ┌───────────▼───────────┐
                    │   MongoDB Database     │
                    ├───────────────────────┤
                    │ • rooms collection     │
                    │ • roommembers          │
                    │ • messages             │
                    │ • users                │
                    └───────────────────────┘
```

---

## 🔐 Security Layer Architecture

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     SECURITY LAYERS (Onion Model)                        │
└─────────────────────────────────────────────────────────────────────────┘

                                 ┌────────────────┐
                                 │   PLAINTEXT    │
                                 │  "Hello group" │
                                 └────────┬───────┘
                                          │
                                          │ ① Client encrypts with
                                          │    Session Key (AES-256-GCM)
                                          │
                    ╔═════════════════════▼════════════════════╗
                    ║         ENCRYPTED MESSAGE                 ║
                    ║  Content: "x8KmL3pNq..."                 ║
                    ║  IV: "a3F2kL..."                         ║
                    ║  AuthTag: "p9NqW7..."                    ║
                    ╚═════════════════════╤════════════════════╝
                                          │
                                          │ ② Sent to server (HTTPS)
                                          │
                    ┌─────────────────────▼────────────────────┐
                    │         SERVER (Never Decrypts)          │
                    │                                           │
                    │  • Stores encrypted content              │
                    │  • Broadcasts to room members            │
                    │  • No access to Session Key              │
                    └─────────────────────┬────────────────────┘
                                          │
                                          │ ③ Broadcast to all members
                                          │
                    ╔═════════════════════▼════════════════════╗
                    ║    ENCRYPTED MESSAGE (Still Encrypted)    ║
                    ║  Each member receives same ciphertext    ║
                    ╚═════════════════════╤════════════════════╝
                                          │
                              ┌───────────┼───────────┐
                              │           │           │
                    ┌─────────▼─────┐  ┌─▼─────┐  ┌─▼─────────┐
                    │  Member A      │  │Member B│  │ Member C  │
                    │                │  │        │  │           │
                    │ ④ Get Session  │  │④ Get   │  │④ Get      │
                    │    Key from    │  │Session │  │Session    │
                    │    storage     │  │Key     │  │Key        │
                    └────────┬───────┘  └───┬────┘  └────┬──────┘
                             │              │             │
                             │ ⑤ Decrypt    │⑤ Decrypt   │⑤ Decrypt
                             │              │             │
                    ┌────────▼───────┐  ┌──▼─────┐  ┌───▼───────┐
                    │  "Hello group" │  │"Hello  │  │"Hello     │
                    │                │  │group"  │  │group"     │
                    └────────────────┘  └────────┘  └───────────┘
                      PLAINTEXT          PLAINTEXT    PLAINTEXT
```

---

## 🔑 Session Key Distribution Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    SESSION KEY DISTRIBUTION                              │
└─────────────────────────────────────────────────────────────────────────┘

STEP 1: Admin Generates Session Key
┌─────────────────────────────────────┐
│          ADMIN (Client)             │
│                                     │
│  SessionKey = Random(32 bytes)      │
│  → "a3F2kL8pQw9rT5vY..."           │
│     ▲                               │
│     │ Stored in Secure Storage      │
│     │ (Plaintext, local only)       │
└─────────────────────────────────────┘

STEP 2: Admin Encrypts for Each Member
┌─────────────────────────────────────┐
│          ENCRYPTION PHASE            │
│                                     │
│  For Member A:                      │
│  → Fetch Member A's Public Key      │
│  → Encrypt SessionKey with RSA-OAEP │
│  → Result: "x8KmL3pNq..."          │
│     (Unique ciphertext for A)       │
│                                     │
│  For Member B:                      │
│  → Fetch Member B's Public Key      │
│  → Encrypt SessionKey with RSA-OAEP │
│  → Result: "p9NqW7kB5..."          │
│     (Unique ciphertext for B)       │
│                                     │
│  For Member C:                      │
│  → Encrypt with C's Public Key      │
│  → Result: "k2BfT9mX1..."          │
│     (Unique ciphertext for C)       │
└─────────────────────────────────────┘

STEP 3: Server Stores Encrypted Copies
┌─────────────────────────────────────────────────────────────────┐
│                    SERVER (MongoDB)                              │
│                                                                  │
│  RoomMember Collection:                                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ { userId: "A", encryptedSessionKey: "x8KmL3pNq..." }     │  │
│  │ { userId: "B", encryptedSessionKey: "p9NqW7kB5..." }     │  │
│  │ { userId: "C", encryptedSessionKey: "k2BfT9mX1..." }     │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ⚠️  Server CANNOT decrypt these!                                │
│      (No private keys on server)                                │
└─────────────────────────────────────────────────────────────────┘

STEP 4: Member Decrypts with Private Key
┌─────────────────────────────────────┐       ┌─────────────────────────────┐
│       MEMBER A (Client)             │       │      MEMBER B (Client)      │
│                                     │       │                             │
│  ① Fetch encrypted copy from server │       │  ① Fetch encrypted copy     │
│     "x8KmL3pNq..."                 │       │     "p9NqW7kB5..."         │
│                                     │       │                             │
│  ② Get Private Key from storage     │       │  ② Get Private Key          │
│     (Never leaves device)           │       │     (Never leaves device)   │
│                                     │       │                             │
│  ③ Decrypt with RSA-OAEP            │       │  ③ Decrypt with RSA-OAEP    │
│     encryptedKey + privateKey       │       │     encryptedKey + privateKey│
│     → SessionKey                    │       │     → SessionKey            │
│     → "a3F2kL8pQw9rT5vY..."        │       │     → "a3F2kL8pQw9rT5vY..." │
│        ▲                            │       │        ▲                    │
│        │ SAME SessionKey!           │       │        │ SAME SessionKey!   │
│        │ (But encrypted differently)│       │        │                    │
│                                     │       │                             │
│  ④ Store in Secure Storage          │       │  ④ Store in Secure Storage  │
│                                     │       │                             │
│  ⑤ Use for encrypting/decrypting    │       │  ⑤ Use for encrypt/decrypt  │
│     group messages                  │       │     group messages          │
└─────────────────────────────────────┘       └─────────────────────────────┘
```

---

## 🔄 Key Rotation Flow (After Kick)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    KEY ROTATION AFTER KICK                               │
└─────────────────────────────────────────────────────────────────────────┘

BEFORE KICK:
┌──────────────────────────────────────────────────────────────────────┐
│  Room: { sessionKeyVersion: 1 }                                       │
│                                                                        │
│  Members: [Admin, User A, User B, User C (to be kicked)]             │
│                                                                        │
│  RoomMembers:                                                          │
│  ┌───────────────────────────────────────────────────────────────┐   │
│  │ Admin:  { encryptedSessionKey: "v1_admin_key",  version: 1 }  │   │
│  │ User A: { encryptedSessionKey: "v1_userA_key",  version: 1 }  │   │
│  │ User B: { encryptedSessionKey: "v1_userB_key",  version: 1 }  │   │
│  │ User C: { encryptedSessionKey: "v1_userC_key",  version: 1 }  │   │
│  └───────────────────────────────────────────────────────────────┘   │
│                                                                        │
│  All members share SAME SessionKey (encrypted differently)            │
└──────────────────────────────────────────────────────────────────────┘
                                   │
                                   │ Admin kicks User C
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│                     KICK ACTION                                       │
│                                                                        │
│  ① Generate NEW SessionKey                                            │
│     oldKey: "a3F2kL8p..." (v1)                                       │
│     newKey: "z9XwK2mP..." (v2) ← Different random key!              │
│                                                                        │
│  ② Encrypt NEW key for REMAINING members only                        │
│     (Exclude User C)                                                  │
│     - Encrypt for Admin  → "v2_admin_key"                            │
│     - Encrypt for User A → "v2_userA_key"                            │
│     - Encrypt for User B → "v2_userB_key"                            │
│                                                                        │
│  ③ Update database                                                    │
│     - Increment sessionKeyVersion: 1 → 2                             │
│     - Remove User C from members[]                                   │
│     - Deactivate User C's RoomMember entry                           │
│     - Update remaining members with new encrypted keys               │
└──────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
AFTER KICK:
┌──────────────────────────────────────────────────────────────────────┐
│  Room: { sessionKeyVersion: 2 }  ← Incremented                       │
│                                                                        │
│  Members: [Admin, User A, User B]  ← User C removed                  │
│                                                                        │
│  RoomMembers:                                                          │
│  ┌───────────────────────────────────────────────────────────────┐   │
│  │ Admin:  { encryptedSessionKey: "v2_admin_key",  version: 2 }  │   │
│  │ User A: { encryptedSessionKey: "v2_userA_key",  version: 2 }  │   │
│  │ User B: { encryptedSessionKey: "v2_userB_key",  version: 2 }  │   │
│  │ User C: { isActive: false, leftAt: Date }  ← Deactivated      │   │
│  └───────────────────────────────────────────────────────────────┘   │
│                                                                        │
│  Remaining members share NEW SessionKey (v2)                          │
│  User C still has OLD SessionKey (v1) → Can't decrypt new messages   │
└──────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
NEW MESSAGE SENT:
┌──────────────────────────────────────────────────────────────────────┐
│  Admin sends: "Secret message after kick"                             │
│                                                                        │
│  Encrypted with NEW SessionKey (v2):                                  │
│  → Content: "encrypted_with_v2_key..."                               │
│                                                                        │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │ Admin:  Decrypt with v2 key → "Secret message..." ✅         │   │
│  │ User A: Decrypt with v2 key → "Secret message..." ✅         │   │
│  │ User B: Decrypt with v2 key → "Secret message..." ✅         │   │
│  │ User C: Decrypt with v1 key → DECRYPTION FAILS! ❌           │   │
│  │         (Auth tag mismatch, wrong key version)                │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                        │
│  ✅ Security Guarantee: Kicked member can't read new messages!        │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 🏛️ Database Schema Relationships

```
┌────────────────────────────────────────────────────────────────────────┐
│                     MongoDB Collections                                 │
└────────────────────────────────────────────────────────────────────────┘

┌──────────────────┐            ┌─────────────────────┐
│      users       │            │       rooms         │
├──────────────────┤            ├─────────────────────┤
│ _id              │◄───────────┤ ownerId (FK)        │
│ username         │            │ name                │
│ email            │            │ passwordHash        │
│ publicKey  ◄─────┼────┐       │ isPasswordProtected │
│ privateKey (❌)  │    │       │ members[] (FK)      │
└──────────────────┘    │       │ sessionKeyVersion   │
         ▲              │       │ createdAt           │
         │              │       └─────────────────────┘
         │              │                 ▲
         │              │                 │
         │              │                 │
         │       ┌──────┴──────┐          │
         │       │  Used for    │          │
         │       │  Encrypting  │          │
         │       │  Session Key │          │
         │       └─────────────┘          │
         │                                │
         │                                │
┌────────┴──────────────┐        ┌────────┴──────────┐
│    roommembers        │        │     messages      │
├───────────────────────┤        ├───────────────────┤
│ _id                   │        │ _id               │
│ roomId (FK)           │        │ sender (FK)       │
│ userId (FK)           │        │ receiver (FK)     │
│ encryptedSessionKey ◄─┼────────┤ roomId (FK)       │
│   (RSA-OAEP)          │        │ content (AES-GCM) │
│ sessionKeyVersion     │        │ iv                │
│ role                  │        │ authTag           │
│ joinedAt              │        │ messageType       │
│ isActive              │        │ timestamp         │
└───────────────────────┘        └───────────────────┘
         │                                ▲
         │                                │
         └────────────────────────────────┘
           "Member uses encrypted key
            to decrypt message content"
```

**Key Points:**
- `users.publicKey`: Used by admin to encrypt Session Key
- `roommembers.encryptedSessionKey`: Session Key encrypted for this specific user
- `messages.content`: Message encrypted with Session Key (AES-GCM)
- Server stores all encrypted, never sees plaintext

---

## 📊 Performance Comparison

```
┌─────────────────────────────────────────────────────────────────────────┐
│              ENCRYPTION PERFORMANCE: 1-1 vs GROUP CHAT                   │
└─────────────────────────────────────────────────────────────────────────┘

SCENARIO: Send message to 100 members

METHOD 1: 1-1 Chat Style (Encrypt per recipient)
┌───────────────────────────────────────────────────────────────┐
│  For each of 100 members:                                     │
│    • Fetch member's public key                                │
│    • Encrypt message with RSA-2048                            │
│    • Time per encryption: ~50ms                               │
│                                                                │
│  Total Time: 100 × 50ms = 5,000ms (5 seconds) ❌              │
│  Network: 100 separate API calls                              │
│  Database: 100 message inserts                                │
└───────────────────────────────────────────────────────────────┘

METHOD 2: Group Chat with Session Key (This Implementation)
┌───────────────────────────────────────────────────────────────┐
│  One-time:                                                     │
│    • Encrypt message with Session Key (AES-256-GCM)           │
│    • Time: ~1ms                                               │
│    • Send to server                                           │
│                                                                │
│  Server:                                                       │
│    • Broadcast to 100 members (Socket.IO)                     │
│    • Time: ~10ms                                              │
│                                                                │
│  Total Time: 1ms + 10ms = 11ms ✅                             │
│  Network: 1 API call + socket broadcast                       │
│  Database: 1 message insert                                   │
│                                                                │
│  PERFORMANCE IMPROVEMENT: 450x faster! 🚀                     │
└───────────────────────────────────────────────────────────────┘

KEY DISTRIBUTION OVERHEAD (One-time cost):
┌───────────────────────────────────────────────────────────────┐
│  When creating group or adding member:                        │
│    • Generate Session Key: 1ms                                │
│    • Encrypt for each member (RSA): 10ms × 100 = 1,000ms     │
│                                                                │
│  Total: ~1 second (one-time)                                  │
│  Amortized over 1000 messages: 1ms per message                │
│                                                                │
│  CONCLUSION: Negligible overhead, huge performance gain! ✅   │
└───────────────────────────────────────────────────────────────┘
```

---

## 🎯 Implementation Status

```
┌──────────────────────────────────────────────────────────────┐
│                    IMPLEMENTATION PROGRESS                    │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  BACKEND (Node.js + MongoDB)                                 │
│  ✅ Room.js model (password, sessionKeyVersion)              │
│  ✅ RoomMember.js model (encryptedSessionKey)                │
│  ✅ groupKeyService.js (key generation/encryption)           │
│  ✅ /api/groups routes (create, join, kick, etc.)            │
│  ✅ Socket.IO handlers (group messages, events)              │
│  ✅ Message.js extended (roomId, iv, authTag)                │
│  ✅ server.js integration                                    │
│                                                               │
│  Progress: 100% ███████████████████████████████              │
│                                                               │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  FRONTEND (Flutter + SQLite)                                 │
│  ✅ room.dart model                                           │
│  ✅ room_member.dart model                                    │
│  ✅ database_helper.dart v3 migration                         │
│  ✅ Database methods (insert/get rooms, members)             │
│  🚧 group_key_service.dart (RSA encryption)                  │
│  🚧 create_group_screen.dart (UI)                            │
│  🚧 join_group_dialog.dart (UI)                              │
│  🚧 group_list_screen.dart (UI)                              │
│  🚧 group_chat_screen.dart (UI)                              │
│  🚧 Socket integration                                        │
│                                                               │
│  Progress: 70%  ████████████████████░░░░░░░░░░               │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

---

**Created:** December 2025  
**Purpose:** Visual reference for Group Chat E2EE architecture  
**Related Docs:** GROUP_CHAT_GUIDE.md, GROUP_CHAT_SUMMARY.md
